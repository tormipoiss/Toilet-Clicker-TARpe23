@* @model Toilet_Clicker.Models.Locations.LocationIndexViewModel

<div>
	@foreach (var Model in Model)
	{
		<partial name="_Images" model="image" />
	}
	@for (int i = 0; i < Model.Image.Count(); i++)
	{
		var image = Model.Image[i];
		var locationID = Model[0].ID;
		switch (i)
		{
			<div>
			case 0:
				<div class="d-flex justify-content-center">
						@* SET IMAGE AS BACKGROUND *@
@* 					<img class="card-img-top" style="width:stretch; height:stretch" src="@Model.Image" asp-append-version="true" />
				</div>
				break;
			case 1:
				<a asp-action="details" asp-controller="locations" asp-route-id="@location.ID" class="btn btn-info">Details</a>
				<partial name="_Images" model="imageTTHK" />
				break;
			case 2:
				// code block
				break;
			case 3:
				// code block
				break;
			case 4:
				// code block
				break;
			default:
				break;
			</div>
		}
	}
</div> *@